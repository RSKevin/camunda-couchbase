package com.camunda.couchbase;

import static org.junit.Assert.assertTrue;
import static org.junit.Assert.fail;

/*@RunWith(SpringRunner.class)
@SpringBootTest
public class MockDataApiApplicationTests {

	@Test
	public void testExtractFromCSV() {
		ExtractionService extractorService = new ExtractionServiceImpl();
		File file = new File("records.csv");
		try {
			List<Record> records = extractorService.extractFromCSV(file);
			assertTrue(!records.isEmpty());
		} catch (IOException e) {
			fail("Error while processing the file. " + e.getMessage());
		} catch (Exception e) {
			fail(e.getMessage());
		}
	}

	@Test
	public void testExtractStatmentFromXML() {
		ExtractionService extractorService = new ExtractionServiceImpl();
		File file = new File("records.xml");
		try {
			List<Record> records = extractorService.extractFromCSV(file);
			assertTrue(!records.isEmpty());
		} catch (IOException e) {
			fail("Error while processing the file. " + e.getMessage());
		} catch (Exception e) {
			fail(e.getMessage());
		}
	}

}*/
